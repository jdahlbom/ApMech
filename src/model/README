This directory contains a autogenerated class representing the
datamodel described in the XSD files.

Makefile should only ever be run when you wish to update the
datamodel serialization code to match a new schema file.
